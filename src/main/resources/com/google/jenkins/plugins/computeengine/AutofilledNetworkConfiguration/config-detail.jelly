<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry field="network" title="${%Network}">
        <f:select/>
    </f:entry>
    <f:entry field="subnetwork" title="${%Subnetwork}">
        <f:select/>
    </f:entry>
</j:jelly>